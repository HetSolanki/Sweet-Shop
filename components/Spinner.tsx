const Spinner = () => {
  return (
    <div
      className={`animate-spin inline-block h-8 w-8 border-[3px] border-current border-t-transparent text-black rounded-full`}
    ></div>
  );
};

export default Spinner;
